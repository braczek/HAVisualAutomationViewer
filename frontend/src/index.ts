/**
 * Entry point for Visual AutoView library
 * Exports the main application component for use as a web component
 */

export { VisualAutoViewApp } from './app';

// Import main to initialize on module load
import './main';
